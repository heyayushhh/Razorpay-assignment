=== BRICK 1: Project Setup & DB Schema ===

 am building a backend for a Razorpay hiring assignment in strict JavaScript (no TypeScript).
Context:

. Framework: Express.js
· Database: PostgreSQL (using
. Auth: Cookie-based JWT
. Port: MUST be 7002

. I have a

Task:

1. Generate package. json with EXACTLY these scripts:
"dev": "node src/index.js"

"db:migrate": "node src/db/migrate.js"

"db: seed-data": "node src/db/seed. js"

2. Create src/config/db. js that creates a pg.Pool using process.env.DATABASE_URL . Export the pool.
3. Create src/index. js that initializes Express, uses express. json() and
listens on process.env.PORT | | 7002
4. Create src/db/migrate. js that reads

gracefully.
5. Create src/db/schema.sq1 based on my attached schema diagram. Include tables: users
approval_audit_log, sessions. Add all CHECK constraints

pg

library, raw SQL preferred over ORM for speed)

.env file with DATABASE_URL

JWT_SECRET, and PORT.

cookie-parser(), connects to DB, and

src/db/schema.sq1 and executes it against the pool. Handle errors

reporting_assignments, reimbursements,
mentioned in the diagram legend.
6. Create src/db/seed. js that inserts ONLY the CFO account: email= cfo@org.com, password=
CFO#ORG@Apri12026 (bcrypt hash), role= CFO. Do NOT seed anything else.
Constraints:
. NO TypeScript files.
· Use async/await everywhere.
. Log success/failure of migrate and seed scripts clearly.
. After generating, tell me the exact commands to test migration and seeding.

=== END BRICK 1 ===

======== BRICK 2 : Authentication Middleware & Onboarding Routes =========

Continuing from previous context. Now implement public onboarding endpoints.
Requirements:
1. Create src/middleware/auth.js
o Extract auth_token from cookies
o Verify JWT using process. env. JWT_SECRET
o Attach decoded user {id, email, role} to req.user
o Return 401 if missing/invalid/expired

2. Create src/routes/onboarding.js with /rest prefix:
- POST /register: Validate email ends with @org.com. Hash password (bcrypt, 10 rounds). Insert user with role='EMP'. Return {status:"success", data:{userId, name, email, role}}.
- POST /login: Validate @org.com. Compare password. Generate JWT (expires 7d). Set cookie: {httpOnly:true, sameSite:'strict', maxAge:7*24*60*60*1000}. Return user data WITHOUT password.
- POST /logout: Clear auth_token cookie. Return success.

3. Register routes in src/index.js.
Edge Cases to Handle:
- Duplicate email registration → 409 Conflict
- Invalid email domain → 400 Bad Request
- Wrong password → 401 Unauthorized (generic message)
- Missing fields → 400 Validation Error
Generate the code. Then provide curl commands to test register, login, and logout flows.

=== END BRICK 2 ===

======= BRICK 3 : Role-Based Access Control Middleware & Employee Management ======

Now implement protected RBAC endpoints.
Requirements:
1. Create src/middleware/roleCheck.js: Factory function requireRole(...roles) that returns middleware checking req.user.role.
2. Create src/routes/employees.js and controller:
   - GET /rest/employees:
    - EMP → 403 Forbidden
    - RM → SELECT users WHERE id IN (SELECT employee_id FROM reporting_assignments WHERE rm_id = req.user.id) AND role='EMP'
    - APE → SELECT users WHERE role IN ('EMP','RM')
    - CFO → SELECT all users
    - Response: {status:"success", data:{users:[{userId,name,email,role}]}}
- POST /rest/employees/assign: CFO only. Body: {userId(emp), userId(rm)}. Insert into reporting_assignments. Validate both users exist and have correct roles.
- DELETE /rest/employees/assign: CFO only. Body: {userId(emp), userId(rm)}. Delete matching row.
3. Create src/routes/roles.js:
    - POST /rest/roles/assign: CFO only. Body: {userId, role}. Update user role. Validate role is one of ['EMP','RM','APE','CFO'].
Critical: All protected routes MUST use auth middleware FIRST, then roleCheck middleware.
Generate code + test curl commands for each role's visibility.

=== END BRICK 3 ===

==== BRICK 4: Reimbursement Creation & Dual-Approval Logic =====

Implement reimbursement endpoints with dual-approval workflow.
Schema Reminder: reimbursements has rm_approved_at, ape_approved_at, status (PENDING/APPROVED/REJECTED).
Requirements:
1. POST /rest/reimbursements: EMP only. Body: {title, description, amount}. Validate amount > 0. Insert with status='PENDING'. Return created reimbursement.
2. PATCH /rest/reimbursements: RM/APE/CFO only. Body: {userId, status} where userId is the reimbursement ID (note: PDF says userId but logically it's reimbursementId - handle both).
APPROVAL STATE MACHINE:
    -  If RM approves: Set rm_approved_at=NOW(), rm_approved_by=req.user.id. Keep status='PENDING' UNLESS ape_approved_at already exists → then set status='APPROVED'.
    -  If APE approves: Check rm_approved_at IS NOT NULL. If yes → set ape_approved_at=NOW(), ape_approved_by=req.user.id, status='APPROVED'. If no → return 422 "RM must approve first".
    -  If anyone rejects: Set rejected_at=NOW(), rejected_by=req.user.id, rejection_reason (if provided), status='REJECTED'. Override any prior approvals.
    -  If CFO approves: Set BOTH rm_approved_at AND ape_approved_at, status='APPROVED'.
3. Log EVERY approval/rejection to approval_audit_log table with actor_role, action, from_status, to_status.
Generate code + comprehensive test scenarios covering all state transitions.

=== END BRICK 4 ===


